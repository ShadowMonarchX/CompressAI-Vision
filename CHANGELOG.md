# Changelog

## Unreleased

- Full structural migration to versioned routers, models, core cross-cutting modules, and service orchestration. This is distinct from prior fix/restructure passes.
- Final verification pass: added the v1 comparison route, shared helpers, and explicit comparison orchestration.
- Migration closure: removed generated bytecode from the working tree, verified clean imports and route registration, adopted shared size formatting in job metrics, added compare endpoint coverage, and aligned OpenAPI tags for v1 plus the placeholder v2 group.
